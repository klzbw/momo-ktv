# PC51 无损 FLAC 分轨转码 —— 交接说明（给 PC51 本地豆包 / 使用者）

## 一、这件事在做什么
把源盘里的整轨 CDImage.ape/wav/flac + .cue，按 cue 逐首切成单曲 FLAC，
统一命名 `歌手-歌名-语言-风格.flac`（序号、专辑写入 FLAC 内部 metadata，不进文件名），并写入歌名/歌手/专辑/风格/语言/音轨标签，
输出到源盘 `all-flacs\` 并**镜像原目录结构**。源文件只读、不删不改。

## 二、路径（关键）
- 源共享：`\\192.168.3.80\music`（SMB，账号 klzbw / Dd112233）= NAS 容器内 `/mv/library2`
- 输出根：`\\192.168.3.80\music\all-flacs`
- 脚本目录：`C:\Users\Administrator\Desktop\ai-worker\`
  - `flac_convert.py` 主程序（只用标准库 + 本机 C:\ffmpeg\bin\ffmpeg.exe）
  - `run_flac.bat` 后台启动入口（日志写到 flac_run.log，UTF-8）
- Windows 网络盘按登录会话隔离：脚本启动时会自己 `net use` 建连，**不依赖手动盘符**。

## 三、当前状态
已用计划任务 `MomoFlacConvert` 在后台全量跑（232 张整轨 / 228 有 cue / 2944 首，10 并发）。
断点续跑：已存在且非空的 flac 自动跳过，中断后重跑不会重复转。

## 四、常用命令（PowerShell，先建 SMB 连接再看产物）
```powershell
net use \\192.168.3.80\music /user:klzbw Dd112233   # 当前会话建连（查文件前先执行）
schtasks /query /tn MomoFlacConvert /fo csv /nh     # 看任务是否 Running
Get-Process python | Measure-Object                 # 看 python 进程数(主+10并发)
# 看日志（必须 -Encoding utf8，否则中文乱码）：
Get-Content C:\Users\Administrator\Desktop\ai-worker\flac_run.log -Tail 30 -Encoding utf8
# 已产出数量：
(Get-ChildItem \\192.168.3.80\music\all-flacs -Recurse -Filter *.flac).Count
# 失败清单（应为空或极少）：
Get-Content \\192.168.3.80\music\all-flacs\_convert_failed.txt -Encoding utf8
```

## 五、停止 / 续跑 / 调并发
- 停止：`schtasks /end /tn MomoFlacConvert`（再 `Get-Process python|Stop-Process -Force` 兜底）
- 续跑：再执行 `schtasks /run /tn MomoFlacConvert`，或直接双击 run_flac.bat（前台看输出）
- 改并发：编辑 run_flac.bat 里 `--workers 10`（本机 20 逻辑核，建议 8~12；
  同时在跑 Demucs 分离时别超过 12，给 GPU worker 和系统留核）
- 只转某张：`python -X utf8 flac_convert.py --mode cue --only 关键字 --smb-user klzbw --smb-pass Dd112233`
  注意：命令行直接传中文在某些远程通道会乱，必要时把专辑路径写进脚本或用 --limit。
- 只规划不转：加 `--dry`。

## 六、完成判据
日志最后出现 `全部完成: ok=.. skip=.. fail=..`，且 `_convert_failed.txt` 为空（或里面的问题已单独处理）。
4 张无 cue 的整轨不会被切（脚本日志标 NO_CUE），需要单独找 cue 或按单曲转，清单：
吴莉-蝴蝶与蓝(dts)、中乐发烧天碟Vol.12、常静-十指弹一、常静-离弦。

## 七、后续尚未做（等整轨跑完再做，勿与本批混在一起）
1. 普通单曲 WAV/APE→FLAC（脚本预留 --mode single，待实现/启用）。
2. 本来就是 FLAC 的单曲“移动”进 all-flacs 镜像目录。
3. 去重：只对“字节完全相同”的真副本清理，先进隔离区观察 7 天，不按歌名盲删。
4. 转完回写 momo 数据库 songs 表：修复 GBK 乱码歌名、把 filepath 指向新 flac。
5. 确认无误后再讨论删除源 WAV（空间只剩约 430G，务必“转-验-隔离-滚动”，不能先全转再删）。

## 八、故障排查
- 日志中文乱码：只是读取编码问题，用 `-Encoding utf8` 读；不影响转码结果。
- 产物计数为 0/列不到目录：当前 PowerShell 会话没建 SMB 连接，先执行第四条的 net use。
- ffmpeg 报错：确认 C:\ffmpeg\bin\ffmpeg.exe 存在；脚本无 ffprobe 时自动用 ffmpeg 解码质检。
- 计划任务跑不起来：`schtasks /create /tn MomoFlacConvert /tr "C:\Users\Administrator\Desktop\ai-worker\run_flac.bat" /sc once /st 23:59 /ru administrator /rp 1235 /f` 再 `/run`。
