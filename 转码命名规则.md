# FLAC 转码命名与识别规则

> 本文件记录所有转码、命名、元数据识别规则。`flac_convert.py` 必须严格遵循本规则。
> 最后更新：2026-09-03

---

## 一、命名格式（四段制）

```
歌手-歌名-语种-风格.flac
```

- **已废弃**：序号、专辑名（目录名已承载专辑信息）
- 分隔符：半角 `-`
- 文件名中的 `-` 替换为全角 `－`
- 非法字符 `\ / : * ? " < > |` 替换为空格
- 总长度（UTF-8 字节）不超过 200，超长时截断歌名

### 示例

| 旧格式（六段） | 新格式（四段） |
|---|---|
| `01-邓丽君-月亮代表我的心-国语-流行-岛国之情歌.flac` | `邓丽君-月亮代表我的心-国语-流行.flac` |
| `03-群星-茉莉花-纯音乐-发烧-HIFI典范.flac` | `群星-茉莉花-纯音乐-民族.flac` |
| `05-未知艺术家-Track05-国语-未知-某专辑.flac` | `（从txt修正歌名和歌手后）.flac` |

---

## 二、歌名识别规则

### 2.1 优先来源
1. **CUE 文件** `TITLE` 字段（每首曲目）
2. **同级/上级 TXT 文件**中的曲目列表（当 cue 歌名为通用名时）
3. 源文件名（single 模式）

### 2.2 必须修正的通用歌名
以下歌名视为无效，必须从 TXT 或网络查找修正：
- `Track01` / `Trackxx` / `trackxx` / `TRACKxx`
- `音轨01` / `音轨xx`
- `未知标题` / `Unknown Title` / `Untitled`

### 2.2.1 网络自动查询修复（TXT 无法修正时）
当同级/上级 TXT 无法提供正确歌名时，**必须自动从网络查询**，流程：
1. 从目录名提取专辑名和歌手名
2. 搜索关键词：`{歌手} {专辑名} 曲目列表` 或 `{专辑名} tracklist`
3. 从搜索结果（美篇、网易云、QQ音乐、豆瓣等）提取完整曲目列表
4. 按 cue 中的曲目序号匹配正确曲名
5. 修复 cue 文件并重新转码该专辑
6. 搜索来源记录到 `_web_fix_log.jsonl`，便于溯源

**搜索优先级**：美篇文章 → 网易云音乐专辑 → QQ音乐 → 豆瓣音乐 → 通用搜索结果

### 2.3 TXT 曲目列表解析
- 搜索范围：当前目录 → 上级目录 → 上上级目录（最多 3 层）
- 匹配模式：`序号. 歌名` / `序号、歌名` / `序号 歌名` / `序号-歌名`
- 序号可以是 `01`、`1.`、`1、`、`(1)` 等格式
- 取解析出曲目最多的那个 TXT

### 2.4 歌名清理
- 移除首尾的网站标记（见第五节）
- 移除首尾的序号前缀（如 `01. `、`1、`）
- 多个空格合并为一个
- 移除首尾的 ` -_·`

---

## 三、歌手识别规则

### 3.1 优先来源
1. **CUE 文件** 曲目级 `PERFORMER` → 专辑级 `PERFORMER`
2. **同级/上级 TXT 文件**中的歌手信息
3. **目录名**推断（如 `邓丽君-岛国之情歌` → 邓丽君）

### 3.2 TXT 歌手关键词
从 TXT 中搜索以下关键词后的内容：
- `歌手`、`艺术家`、`表演者`、`演奏者`、`指挥`
- `Artist`、`Performer`、`Conductor`
- `专辑艺人`、`唱片艺人`

### 3.3 必须清理的无效歌手
- `未知` / `未知艺术家` / `Unknown Artist` / `Unknown`
- 含网站信息的歌手名（见第五节）
- 含收藏标记的歌手名：
  - `群星[残阳收藏]` → `群星`
  - `群星【小雨收藏】` → `群星`
  - `倦鸟馀花论坛` → 从 TXT/目录重新推断
  - `炫音论坛` → 从 TXT/目录重新推断

### 3.4 合辑处理
- 多歌手合辑：歌手统一为 `群星`
- 单歌手合辑/精选：保留实际歌手名

### 3.5 从歌名括号提取歌手（群星合辑）
当歌手为 `群星` 且歌名中包含括号时，检查括号内容：
- **括号内是歌手名**（如 `(孙露)`、`(张玮伽)`、`(慕容晓晓 肖玄)`、`(降央卓玛VS小曾)`）→ 提取为歌手名，从歌名中移除括号
- **括号内是"童声：xxx"格式** → 提取冒号后的名字为歌手
- **括号内是版本说明**（含 `版`、`原创`、`国语`、`粤语`、`伴奏`、`Demo`、`Remix`、`Live`、`对比`、`新编`、`精选`、`现场`、`演唱会` 等关键词）→ 保留在歌名中，不提取

---

## 四、语种识别规则

### 4.1 推断逻辑
从 `歌手 + 歌名 + 专辑名 + 目录名` 组合文本中推断：

| 关键词 | 语种 |
|---|---|
| 粤语、Cantonese、粤 | 粤语 |
| 英语、English、英文 | 英语 |
| 日语、Japanese、日文 | 日语 |
| 韩语、Korean、韩文 | 韩语 |
| 纯音乐、轻音乐、New Age、Instrumental、演奏曲 | 纯音乐 |
| 其他（含中文字符） | 国语 |

### 4.2 默认值
- 含人声且无法确定 → `国语`
- 纯演奏/乐器 → `纯音乐`

---

## 五、网站信息清理规则

### 5.1 必须移除的网站标记
- `【WWW.JPHiFI.COM】`、`[www.1qt8.com]`、`（hifidiy.net）`
- 任何域名：`www.xxx.com`、`xxx.cn`、`xxx.net` 等
- 论坛名：`炫音论坛`、`倦鸟馀花论坛`、`捌零论坛` 等

### 5.2 应用范围
- 歌名、歌手名、专辑名中的网站信息全部移除
- 原始歌名保留在 FLAC metadata 的 `COMMENT` 字段（`Original: 原始文件名`）

### 5.3 域名正则
匹配常见后缀：`com|cn|net|org|cc|tv|io|me|co|info|biz|top|xyz|vip|club|site|online|store|tech|app|dev|cloud|zone|fm|radio`

---

## 六、风格识别规则

### 6.1 优先来源
1. **CUE 文件** `GENRE` 字段（需过滤无效值）
2. **目录名/专辑名**关键词匹配
3. 按语种默认

### 6.2 无效 GENRE 值（必须过滤，不能作为风格）
以下是唱片制作方式或来源标记，**不是音乐风格**：
- `发烧`、`HIFI`、`HQCD`、`DSD`、`K2HD`、`XRCD`
- `LP`、`黑胶`、`SACD`、`HDCD`、`LPCD`、`SHMCD`
- `UPM`、`AQCD`、`BSCD`、`金碟`、`银碟`、`母带`
- `正版`、`原抓`、`Other`、`Unknown`、`未知`
- `论坛`、`炫音`、`倦鸟`、`收藏`、`残阳`、`小雨`

### 6.3 风格关键词映射（从目录名/专辑名推断）

| 关键词 | 风格 |
|---|---|
| 古典、交响曲、协奏曲、奏鸣曲、贝多芬、莫扎特、巴赫、肖邦 | 古典 |
| 钢琴、小提琴、大提琴、萨克斯、纯音乐、轻音乐、New Age、新世纪 | 纯音乐 |
| 民乐、民族、古筝、二胡、古琴、琵琶、笛子、箫、唢呐、扬琴 | 民族 |
| 草原、高原、蒙古、西藏、新疆、傣族、苗族、藏族、维吾尔 | 民族 |
| 流行、Pop、情歌、爱情、粤语、台语、闽南语 | 流行 |
| 摇滚、Rock、金属、Metal | 摇滚 |
| 爵士、Jazz、蓝调、Blues | 爵士 |
| 电子、Electronic、DJ、舞曲、Dance、Remix | 电子 |
| 民谣、Folk、校园 | 民谣 |
| R&B、节奏布鲁斯、灵魂、Soul | R&B |
| 嘻哈、Hip-Hop、说唱、Rap | 嘻哈 |
| 乡村、Country | 乡村 |
| 原声、OST、Soundtrack、影视 | 原声 |
| 儿歌、儿童、童谣 | 儿童 |
| 佛歌、佛乐、禅乐、宗教、基督 | 宗教 |
| 老歌、经典、怀旧 | 经典 |
| 试音、试机 | 试音 |
| 群星、合辑、合集、精选 | 合辑 |
| 环绕、DTS、5.1 | 环绕 |

### 6.4 默认值
- 纯音乐/乐器类 → `纯音乐`
- 其他（含人声） → `流行`

---

## 七、整轨识别规则

### 7.1 整轨目录判定
目录中存在 `.cue` 文件，且 cue 为**单 FILE 格式**（一个整轨音频文件 + 多个 TRACK 索引）。

### 7.2 整轨音频文件命名
**不局限于 `CDImage.*`**，以下命名均视为整轨：
- `CDImage.wav` / `CDImage.ape` / `CDImage.flac`
- `群星 - 千年好歌精选CD1.wav`
- `专辑名.wav` / `艺术家 - 专辑名.ape`
- 任何被 cue 的 `FILE` 字段引用的音频文件

### 7.3 排除：多 FILE 格式 cue
如果 cue 中有多个 `FILE` 行，说明是单曲索引（每首一个独立文件），不是整轨，交给 `--mode single` 处理。

### 7.4 支持的整轨格式
`.wav`、`.ape`、`.flac`、`.tta`、`.wv`

---

## 八、元数据写入规则

每首输出 FLAC 必须写入以下 metadata：

| 字段 | 内容 |
|---|---|
| `TITLE` | 歌名 |
| `ARTIST` | 歌手 |
| `ALBUM` | 专辑名（从 cue 或目录名推断） |
| `GENRE` | 风格 |
| `LANGUAGE` | 语种 |
| `TRACKNUMBER` | 曲目序号 |
| `COMMENT` | `Original: 原始文件名`（保留溯源信息） |

音频流使用 `-c:a copy` 无损复制，不重新编码。

---

## 九、输出目录结构

```
\\192.168.3.80\music\all-flacs\
├── 邓丽君\
│   ├── 岛国之情歌\
│   │   ├── 邓丽君-月亮代表我的心-国语-流行.flac
│   │   └── ...
│   └── ...
├── 群星\
│   └── 发烧合辑\
│       └── ...
├── _convert_log.jsonl      # 转换日志
└── _convert_failed.txt     # 失败清单
```

输出目录镜像源目录的相对路径结构。

---

## 十、并发与性能

- **推荐并发数**：20（Xeon E5-2680 v2 / 10核20线程）
- **调整依据**：CPU 使用率 < 60% 时可增加；> 85% 时减少
- **启动方式**：Windows 计划任务 `MomoFlacConvert`，执行 `run_flac.bat`
- **日志**：`flac_run.log`（本地）+ `_convert_log.jsonl`（NAS）

---

## 十一、失败处理

- 转换失败的曲目写入 `_convert_failed.txt`
- 已存在且非空的输出文件自动跳过（支持断点续转）
- 整轨切割失败时记录具体曲目序号和错误信息
- 无 CUE 的整轨专辑：人工确认后用 `--mode single` 逐个转换，或手动补充 cue
