#!/bin/bash
# 安装前执行：预创建 data / mv / AI 模型目录，避免共享目录尚未就绪时首次挂载报错。
source "$(dirname "$0")/lib_settings.sh"

mkdir -p "$(default_data_path)"
mkdir -p "$(default_mv_path)"
mkdir -p "$(default_data_path)/ai-models"

exit 0
